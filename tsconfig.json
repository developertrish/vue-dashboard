{
  "compilerOptions": {
    // ... other compiler options
    "baseUrl": ".", // This is important so TypeScript knows where to resolve paths from
    "paths": {
      "@/*": [
        "./src/*" // This alias means that anything starting with @/ will look inside the src directory
      ]
    }
  },
  "files": [],
  "references": [
    {
      "path": "./tsconfig.node.json"
    },
    {
      "path": "./tsconfig.app.json"
    }
  ]
}
